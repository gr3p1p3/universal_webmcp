interface:
  display_name: "Universal WebMCP Runtime"
  short_description: "Route interactive browser tasks through WebMCP"
  default_prompt: "Use $universal-webmcp-runtime to run a cheap WebMCP capability gate, then complete this interactive browser task through compact tools when available."

policy:
  allow_implicit_invocation: true
